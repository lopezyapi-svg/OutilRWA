fastapi==0.115.6
uvicorn[standard]==0.32.1
PyJWT==2.10.1
bcrypt==4.2.1
pydantic>=2.11
openpyxl==3.1.5
python-multipart==0.0.27
sqlalchemy==2.0.36
numpy>=1.26
scipy>=1.11.0
pandas>=2.1.0

# Rendu PDF du classeur officiel FODEP (app/fodep/xlsx_pdf.py), sans Excel ni
# LibreOffice : sans cette ligne l'export PDF marche en dev (installe a la
# main) et casse silencieusement dans Docker et l'installeur Windows.
reportlab==5.0.1

# Requis par fastapi.testclient : sans lui, quatre fichiers de tests ne se
# collectent meme pas et la suite passe pour verte alors qu'elle n'a rien
# execute.
httpx>=0.27.0
